const Discord = module.require("discord.js");
let profile = require('./profile.json');
const fs = require("fs");
module.exports.run = async (bot , message , args) => {
    let userWarns = profile.warns;
    
};

module.exports.help = {
   
    
}